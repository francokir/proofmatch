import type { ProofFlowStep } from '../domain/private-match';
export function ProofTimeline({ steps }: { steps: ProofFlowStep[] }) { return <ol className="proof-timeline" aria-label="Private compatibility check progress">{steps.map((step, index) => <li className={`proof-timeline-step proof-timeline-step--${step.status}`} key={step.id} aria-current={step.status === 'active' ? 'step' : undefined}><span className="proof-marker">{step.status === 'completed' ? '✓' : step.status === 'failed' ? '×' : index + 1}</span><div><strong>{step.label}</strong><small>{step.status === 'completed' ? 'Completed in demo' : step.status === 'active' ? 'In progress' : step.status === 'failed' ? 'Needs attention' : 'Pending'}</small></div></li>)}</ol>; }
