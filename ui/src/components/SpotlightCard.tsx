import { useRef, type CSSProperties, type ReactNode } from 'react';
interface SpotlightCardProps { children: ReactNode; className?: string; spotlightColor?: string; }
export function SpotlightCard({ children, className = '', spotlightColor = 'rgba(177, 169, 242, 0.18)' }: SpotlightCardProps) { const ref = useRef<HTMLDivElement>(null); const move = (event: React.MouseEvent<HTMLDivElement>) => { const rect = ref.current?.getBoundingClientRect(); if (!rect || !ref.current) return; ref.current.style.setProperty('--mouse-x', `${event.clientX - rect.left}px`); ref.current.style.setProperty('--mouse-y', `${event.clientY - rect.top}px`); }; return <div ref={ref} onMouseMove={move} style={{ '--spotlight-color': spotlightColor } as CSSProperties} className={`spotlight-card ${className}`}>{children}</div>; }
