import { PillButton } from '../components/PillButton';
import { SpotlightCard } from '../components/SpotlightCard';
import type { MatchPassProps } from '../domain/private-match';
export function MatchPassScreen({ data, onBackToJob, onRecruiterView, onLedgerView }: MatchPassProps) { return <main className="match-pass-page" aria-labelledby="pass-title"><header className="pass-intro"><span className="demo-flow-badge">Demo view</span><p>Match found</p><h1 id="pass-title">Your private terms are compatible with this opportunity.</h1><small>Visual preview — real verified match data will be connected to the Midnight integration.</small></header><SpotlightCard className="match-pass-card"><div className="pass-card-top"><span>ProofMatch</span><b>Match pass</b></div><div className="pass-success">✓</div><p className="pass-demo">Expected successful state shown for UI demonstration.</p><h2>{data.title}</h2><p className="pass-company">{data.company} · {data.location}</p><section className="pass-checks"><span>What was checked</span>{data.checks.map((check) => <p key={check}>✓ {check}</p>)}</section><section className="pass-private"><span>Stayed private</span>{data.privacyItems.map((item) => <p key={item}>{item}<b>Private ⌾</b></p>)}</section><section className="pass-reference"><span>Match reference</span><strong>{data.reference}</strong><small>Demo data · not from Midnight or ledger</small></section></SpotlightCard><section className="privacy-receipt"><h2>Privacy receipt</h2><article><span>Recruiter receives</span><p>✓ Compatibility result</p><p>✓ Match reference after successful registration</p></article><article><span>Recruiter does not receive</span><p>⌾ Exact minimum compensation</p><p>⌾ Exact weekly availability</p><p>⌾ Candidate secret</p></article></section><div className="pass-actions"><button className="secondary-action" type="button" onClick={onBackToJob}>Back to job</button><PillButton onClick={onRecruiterView}>Recruiter view <span aria-hidden="true">→</span></PillButton><button className="ledger-link" type="button" onClick={onLedgerView}>View Ledger Lens</button></div></main>; }
