# ProofMatch UI

Standalone visual shell for ProofMatch. It contains no Midnight integration,
wallet logic, or live data.

```bash
cd ui
npm install
npm run dev
```

To validate a production build:

```bash
npm run build
```
